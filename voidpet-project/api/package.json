{
  "name": "voidpet-api",
  "version": "1.0.0",
  "description": "Azure Functions API for Voidpet",
  "main": "dist/functions/index.js",
  "scripts": {
    "prebuild": "echo '>>> PREBUILD SCRIPT FOR API IS RUNNING <<<'",
    "build": "tsc",
    "postbuild": "echo '--- Contents of API dist folder (postbuild) ---' && ls -R dist && echo '--- End of API dist folder contents ---'",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.7.0",
    "@azure/storage-blob": "^12.27.0"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.10",
    "typescript": "^4.9.5"
  }
}
